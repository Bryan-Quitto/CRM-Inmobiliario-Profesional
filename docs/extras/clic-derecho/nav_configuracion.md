# Reporte de Navegación via onClick - Configuración

**Directorio escaneado:** `C:\Users\THINKPAD\Desktop\CRM Inmobiliario Profesional\CRM_Inmobiliario_Web\src\features\configuracion`

Tras realizar el escaneo exhaustivo de los componentes dentro del módulo de configuración, **no se encontraron instancias** de elementos que utilicen el evento `onClick` (o similares como botones o `div`s) para realizar navegación programática en lugar de utilizar etiquetas `<a>` o `<Link>`.

La única navegación dinámica que ocurre en el módulo se da en `ConfiguracionLayoutMobile.tsx` a través del evento `onChange` en un menú desplegable (`<select>`), lo cual es un patrón estándar para menús de navegación en vista móvil.

**Resultado del escaneo:** 0 incidencias.
